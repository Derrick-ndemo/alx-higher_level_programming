#!/usr/bin/python3

def element_at(my_list, idx):
    my_list = []
    idx = int(idx)

    if idx < 0:
        return None
    elif idx > len(idx) + 1;
        return None
    else:
        return my_list[idx]
